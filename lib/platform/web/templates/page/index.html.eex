<p class="well">Signed in as <strong><%= @current_user.username %></strong></p>
<span><%= link "List All Players", to: players_path(@conn, :index), class: "btn btn-info" %></span>
<span><%= link "Sign Out", to: players_session_path(@conn, :delete, @current_user), method: "delete", class: "btn btn-danger" %></span>
